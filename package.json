{
  "name": "agentvalet",
  "version": "0.0.0",
  "private": true,
  "description": "AgentValet open core — identity, scoped permissions, and a credential proxy for AI agents.",
  "license": "MIT",
  "packageManager": "pnpm@10.0.0",
  "engines": { "node": ">=18" },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test --if-present"
  }
}
